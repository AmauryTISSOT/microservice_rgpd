<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Les DOUBLURES partagées par plusieurs projets de test, et rien d'autre : ni test, ni fixture.
    Une doublure recopiée d'un projet à l'autre finirait par dire deux choses différentes du même
    port — et un test navigateur éprouverait alors un autre moteur que le test fonctionnel.

    Ce n'est pas un projet de test : il ne compte pas parmi les cinq.
  -->

  <PropertyGroup>
    <!-- TargetFramework, ImplicitUsings et Nullable viennent de Directory.Build.props -->
    <IsPackable>false</IsPackable>
    <IsTestProject>false</IsTestProject>
  </PropertyGroup>

  <ItemGroup>
    <!-- Le jeu figé du contrôle d'équivalence d'A2 : ce n'est pas une fixture de test mais la matière
         du double d'Ollama, qui la sert à la place d'un encodeur. Produit par a2_equivalence.py. -->
    <EmbeddedResource Include="Ollama\a2-equivalence.json" LogicalName="a2-equivalence.json" />
  </ItemGroup>

  <ItemGroup>
    <!-- Le système hôte factice écoute sur un port réel, sous Kestrel : le vrai client HTTP du
         service l'appelle, et rien n'est remplacé dans son conteneur. -->
    <FrameworkReference Include="Microsoft.AspNetCore.App" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\MicroserviceRgpd.Core\MicroserviceRgpd.Core.csproj" />
  </ItemGroup>

</Project>
